# Convex

This directory is reserved for the FounderFile backend:

- schema
- queries
- mutations
- actions
- scheduled jobs
- auth integration

The planning docs already define the first-pass schema and function groups.
